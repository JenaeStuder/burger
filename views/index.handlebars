<h1>BURGERS!</h1>

<h2>Burgers!</h2>

<ul>
  {{#each burger_name}}
    {{#unless devoured}}
      {{> burgers/burger_name devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers devoured!</h2>

<ul>
  {{#each burger_name}}
    {{#if devoured}}
      {{> burgers/burger_name devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add a Burger</button>
</form>